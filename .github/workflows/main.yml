name: "shannu1st-wflow"

on : workflow_dispatch
jobs: 
  terraform-job:
    runs-on: Rocket
    steps:
    - name: Checkout
      uses: actions/checkout@v5.0.0
    - name: Install Terraform
      uses: little-core-labs/install-terraform@v2.0.0
      with:
    # Version of terraform to download
       version: 1.13.0
          
      

      
     
    
     
